/*
 * wifi_secrets.h.example - Template for WiFi credentials
 * 
 * Copy this file to wifi_secrets.h and fill in your actual credentials.
 * wifi_secrets.h is gitignored and will not be committed.
 */

#pragma once

typedef struct {
    const char *ssid;
    const char *password;
} wifi_cred_t;

/* Add your WiFi networks here - tried in order from first to last */
static const wifi_cred_t WIFI_CREDENTIALS[] = {
    {"YourHomeWiFi", "your_home_password"},
    {"YourOfficeWiFi", "your_office_password"},
    /* Add more networks as needed */
};

#define WIFI_CRED_COUNT (sizeof(WIFI_CREDENTIALS) / sizeof(WIFI_CREDENTIALS[0]))

